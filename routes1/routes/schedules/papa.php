<?php
    if ($_SERVER['REQUEST_METHOD'] == 'POST'){
        $id = $_POST['id'];

        $sql = "SELECT * FROM users WHERE id = '$id'";
        "I don't have a credit card to sign in on the coursera courses of big data certification but the manager is watching me and I can't do anything. I hope that father will comme safe and sure with the protection of ALLAH";
    }
?>